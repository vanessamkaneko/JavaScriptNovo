//Prototypes

/* A diferença entre um método DIRETO NO OBJETO e um método no PROTOTYPE DO OBJETO, é que cada método que está dentro do objeto, 
é exclusivo daquele objeto. Isso quer dizer que se eu tiver 100 objetos iguais, cada um dos 100 objetos terão o seu próprio método.

Já no prototype, o método é compartilhado por todas as instâncias do objeto, você tem apenas uma referência
do método dentro do objeto e a palavra this apontada para o objeto que estiver usando o método.*/ 